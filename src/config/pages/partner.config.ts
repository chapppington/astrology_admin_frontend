class PartnerPages {
  HOME = "/profile";
  PAYMENTS = `${this.HOME}/payments`;
  WITHDRAWALS = `${this.HOME}/withdrawals`;
  REFERRALS = `${this.HOME}/referrals`;
  SETTINGS = `${this.HOME}/settings`;
}

export const PARTNER_PAGES = new PartnerPages();
