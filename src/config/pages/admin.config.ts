class AdminPages {
  HOME = "/dashboard";
  USERS = `${this.HOME}/users`;
  PARTNERS = `${this.HOME}/partners`;
  PAYMENTS = `${this.HOME}/payments`;
  STORIES = `${this.HOME}/stories`;
  WITHDRAWALS = `${this.HOME}/withdrawals`;
}

export const ADMIN_PAGES = new AdminPages();
